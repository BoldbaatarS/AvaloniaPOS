<Styles xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <Style Selector="Button">
        <Setter Property="FontSize" Value="18"/>
        <Setter Property="Background" Value="#f2f2f2"/>
        <Setter Property="CornerRadius" Value="8"/>
        <Setter Property="Padding" Value="10,10"/>
        <Setter Property="IsVisible" Value="True" />
        <Setter Property="Opacity" Value="1.0" />   
    </Style>

    <Style Selector="Button.pin-button">
    <Setter Property="FontSize" Value="20"/>
    <Setter Property="HorizontalContentAlignment" Value="Center"/>
    <Setter Property="VerticalContentAlignment" Value="Center"/>
    <Setter Property="Width" Value="60"/>
    <Setter Property="Height" Value="60"/>
    
    <Setter Property="CornerRadius" Value="12"/>
    <Setter Property="FontWeight" Value="Bold"/>

    <Setter Property="Background">
        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#f2f2f2" Offset="0"/>
            <GradientStop Color="#d9d9d9" Offset="1"/>
        </LinearGradientBrush>
    </Setter>

    <Setter Property="BorderBrush" Value="#888"/>
    <Setter Property="BorderThickness" Value="1.5"/>

    

    </Style>

    <Style Selector="Button.primary-button1">
    <Setter Property="FontSize" Value="18"/>
    <Setter Property="Height" Value="45"/>
    <Setter Property="CornerRadius" Value="8"/>
    <Setter Property="FontWeight" Value="SemiBold"/>
    <Setter Property="HorizontalContentAlignment" Value="Center"/>
    <Setter Property="VerticalContentAlignment" Value="Center"/>

    <!-- Алтан градиент -->
    <Setter Property="Background">
        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="#fff9c4" Offset="0"/>
        <GradientStop Color="#fbc02d" Offset="1"/>
        </LinearGradientBrush>
    </Setter>

    <Setter Property="BorderBrush" Value="#c0a000"/>
    <Setter Property="BorderThickness" Value="1.5"/>
    </Style>

    <Style Selector="Button.primary-button">
        <Setter Property="Background" Value="#1976D2"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="CornerRadius" Value="8"/>
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="Padding" Value="8,4"/>
    </Style>

    <Style Selector="Button.secondary-button">
        <Setter Property="Background" Value="#EEEEEE"/>
        <Setter Property="Foreground" Value="Black"/>
        <Setter Property="CornerRadius" Value="8"/>
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="Padding" Value="8,4"/>
    </Style>


    <Style Selector="Button.grow-button">
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Transitions">
            <Transitions>
                <DoubleTransition Property="FontSize" Duration="0:0:0.2"/>
            </Transitions>
        </Setter>
    </Style>

    <Style Selector="Button.grow-button:pointerover">
        <Setter Property="FontSize" Value="30"/>
    </Style>

    <Style Selector="Button.bottom-nav-btn">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
    </Style>

    <Style Selector="Button.bottom-nav-btn:pointerover">
        <Setter Property="Background" Value="#444"/>
    </Style>

    <Style Selector="Button.bottom-nav-btn:pressed">
        <Setter Property="Background" Value="#666"/>
    </Style>

        <Style Selector="Button.menu-btn">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Padding" Value="10"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="Cursor" Value="Hand"/>
    </Style>

    <Style Selector="Button.menu-btn:pointerover">
        <Setter Property="Background" Value="#34495E"/>
    </Style>

    

</Styles>
